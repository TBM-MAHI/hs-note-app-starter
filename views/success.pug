doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Success
    link(href="https://fonts.googleapis.com/css2?family=Lexend:wght@400;700&display=swap" rel="stylesheet")
    style.
      body {
        font-family: 'Lexend', sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        flex-direction: column;
      }
      table {
        border-collapse: collapse;
        width: 50%;
        text-align: left;
      }
      th, td {
        padding: 10px;
        border: 1px solid #ddd;
      }
      th {
        background-color: #f2f2f2;
      }
      h2 {
        margin-bottom: 20px;
      }
  body
    h1 Note Master is successfully installed on portal #{accInfo.portalId} 🥳🙌
    h4 Account Information: 
    table
      tr
        th Portal ID
        td= accInfo.portalId
      tr
        th Account Type
        td= accInfo.accountType
      tr
        th Time Zone
        td= accInfo.timeZone
      tr
        th Company Currency
        td= accInfo.companyCurrency
      tr
        th UTC Offset
        td= accInfo.utcOffset
      tr
        th UI Domain
        td= accInfo.uiDomain
      tr
        th Data Hosting Location
        td= accInfo.dataHostingLocation
